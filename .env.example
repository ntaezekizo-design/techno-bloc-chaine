# Database (Render Free MySQL via PlanetScale or Railway)
DATABASE_URL=mysql://user:password@host:3306/cryptomine

# OR individual vars (if not using DATABASE_URL)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=cryptomine
DB_USER=root
DB_PASS=

# Server
PORT=3000
NODE_ENV=production
